// src/pages/workflows.js
import React from "react";
import Layout from "../components/layout";

const Workflows = () => (
  <Layout>
    <h1>Workflows</h1>
    <p>Contenido de la página de Workflow.</p>
  </Layout>
);

export default Workflows;
